{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "npm run",
            "type": "shell",
            "command": "npm",
            "args": [
                "run",
                "${input:RunTask}"
            ]
        }
    ],
    "inputs": [
        {
            "type": "pickString",
            "id": "RunTask",
            "description": "npm run $cmd",
            "options": [
                "test",
                "test:e2e",
            ],
            "default": "test:e2e"
        }
    ]
}
